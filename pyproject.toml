[project]
name = "modelchoice-mcp"
version = "0.0.30"
description = "An open Model Context Protocol server for Vose Software's ModelChoice decision-tree add-in for Excel."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Vose Software" }]
dependencies = [
    # The upper bound is for people INSTALLING the package: uv.lock pins exact
    # versions for this repo's own CI, but a `pip install modelchoice-mcp` resolves
    # against these constraints alone. mcp 2.0 moved `mcp.server.fastmcp`, so an
    # uncapped floor hands end users a build that cannot import its own server.
    # Widen this deliberately, with a migration, not by accident.
    "mcp>=1.2.0,<2",
    "pydantic>=2.0",
    "xlwings>=0.30",
]

[project.scripts]
modelchoice-mcp = "modelchoice_mcp.__main__:main"

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.6",
    "mypy>=1.10",
    "pyinstaller>=6.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/modelchoice_mcp"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "N", "RUF"]

[tool.mypy]
python_version = "3.11"
strict = true
files = ["src"]
